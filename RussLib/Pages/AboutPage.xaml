<Page
    x:Class="RussLib.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SandboxUWPApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="PART_NavigationPanel" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button 
                x:Name="BackButton"
                Click="OnBackButtonClicked"
                Foreground="{ThemeResource ResourceKey=SystemColorButtonTextColor}" 
                Background="{ThemeResource ResourceKey=SystemColorHighlightColor}">
                <Button.Content>
                    <SymbolIcon Symbol="Back"/>
                </Button.Content>
            </Button>
        </StackPanel>-->
        <ContentPresenter x:Name="PART_Content" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>

</Page>
